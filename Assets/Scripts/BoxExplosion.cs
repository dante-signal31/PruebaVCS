using UnityEngine;

public class BoxExplosion : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        InitBox();
    }

    // Update is called once per frame
    void Update()
    {
        Explosion();
    }

    public void InitBox()
    {
        // Box internal initialization.
    }

    public void Explosion()
    {
        // Explodes when certain condition is met.
    }
}
